# `/watch` — 條件成立時主動通知，或直接接手做事（限擁有者）

> 本目錄由指令樹產生，**唯一真實來源是程式碼**；bot 內建的 `/help` 才是
> 即時的那一份。指令一律是原生斜線指令——打 `/` 就會自動補全，參數在送出
> 前就有型別與值域檢查。
>
> **回覆一律泛用**：不提外部服務名稱、不露出主機路徑或檔名、不回傳原始
> 錯誤字串，完整細節只進 log。寫新指令時要沿用。

🔒 **限頻道**：只在 `bot_config.json` 的 `channel_id` 所指頻道回應（**擁有者可跨頻道**）。

共 **10** 個子指令。

> ⚠️ `--then` 帶主機指令的那條路徑**另外硬綁擁有者**——它等於延後執行的 `/host sh run`，閘門必須一樣硬。其餘 `/watch` 子指令走一般角色閘。

---

## 直接子指令

### `/watch clip [text] [run] [then]`

等剪貼簿變動（給文字就等它含那段）

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | — | 要等的內容（留空 = 內容變了就通知） |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |

### `/watch job <id> [run] [then]`

等某個背景作業結束

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `id` | 整數（1–99999） | ✅ | 作業編號 |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |

### `/watch list`

列出進行中的監看

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/watch pixel <x> <y> <color> [run] [then]`

等某一點變成指定顏色

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `x` | 整數（-65535–65535） | ✅ | X 座標 |
| `y` | 整數（-65535–65535） | ✅ | Y 座標 |
| `color` | 文字 | ✅ | #RRGGBB |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |

### `/watch port <target> [run] [then]`

等某個埠接得上

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `target` | 文字 | ✅ | `<埠>` 或 `<主機:埠>` |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |

### `/watch process <name> [run] [then]`

等某個程式開始執行

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 程式名稱 |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |

### `/watch stop <id>`

停掉某個監看

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `id` | 整數（1–99999） | ✅ | 監看編號 |

### `/watch text <text> [run] [then]`

等某段文字出現在畫面上

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要等的文字 |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |

### `/watch ui <name> [run] [then]`

等某個元素出現

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 元素名稱 |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |

### `/watch window <substr> [run] [then]`

等某個視窗出現

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `substr` | 文字 | ✅ | 視窗標題片段 |
| `run` | 文字 | — | 成立時執行的巨集 |
| `then` | 文字 | — | 成立時執行的指令（限擁有者） |
