# `/input` — 鍵盤與滑鼠輸入（限擁有者）

> 本目錄由指令樹產生，**唯一真實來源是程式碼**；bot 內建的 `/help` 才是
> 即時的那一份。指令一律是原生斜線指令——打 `/` 就會自動補全，參數在送出
> 前就有型別與值域檢查。
>
> **回覆一律泛用**：不提外部服務名稱、不露出主機路徑或檔名、不回傳原始
> 錯誤字串，完整細節只進 log。寫新指令時要沿用。

🔒 **限頻道**：只在 `bot_config.json` 的 `channel_id` 所指頻道回應（**擁有者可跨頻道**）。

共 **16** 個子指令。

---

## 直接子指令

### `/input click <x> <y> [button] [window] [background] [shot]`

在指定座標按一下

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `x` | 整數（-65535–65535） | ✅ | X 座標 |
| `y` | 整數（-65535–65535） | ✅ | Y 座標 |
| `button` | 選項：`左鍵` / `右鍵` / `中鍵` | — | 要按哪個鍵（預設 `left`） |
| `window` | 文字 | — | 改用相對此視窗左上角的座標（視窗一移動絕對座標就全錯） |
| `background` | 是／否 | — | 送給背景視窗、不搶焦點（盡力而為） |
| `shot` | 是／否 | — | 完成後附一張截圖 |

### `/input hotkey <combo>`

送出快捷鍵組合

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `combo` | 文字 | ✅ | `+` 分隔，例如 ctrl+s 或 alt+f4 |

### `/input type <text>`

把文字原樣輸入到焦點視窗

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要輸入的字串（打不出中日文，那用 /clip paste） |

## `/input key` — 按住不放（狀態留在主機上，300 秒自動放開）

### `/input key clear`

放開全部按住的鍵

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/input key down <name>`

按住某個鍵不放

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 鍵名，例如 shift / ctrl / a |

### `/input key press <name> [window]`

按一下某個鍵

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 鍵名 |
| `window` | 文字 | — | 送給背景視窗、不搶焦點（盡力而為） |

### `/input key status`

目前按住哪些鍵、各多久

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/input key up <name>`

放開某個鍵

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 鍵名 |

## `/input mouse` — 滑鼠

### `/input mouse click [button] [x] [y]`

按一下（不給座標就點目前位置）

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `button` | 選項：`左鍵` / `右鍵` / `中鍵` | — | 要按哪個鍵（預設 `left`） |
| `x` | 整數（-65535–65535） | — | X 座標（可省略） |
| `y` | 整數（-65535–65535） | — | Y 座標（可省略） |

### `/input mouse dclick <x> <y> [button]`

雙擊

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `x` | 整數（-65535–65535） | ✅ | X 座標 |
| `y` | 整數（-65535–65535） | ✅ | Y 座標 |
| `button` | 選項：`左鍵` / `右鍵` / `中鍵` | — | 要按哪個鍵（預設 `left`） |

### `/input mouse down [button] [x] [y]`

按住滑鼠鍵不放

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `button` | 選項：`左鍵` / `右鍵` / `中鍵` | — | 要按住哪個鍵（預設 `left`） |
| `x` | 整數（-65535–65535） | — | X 座標（可省略） |
| `y` | 整數（-65535–65535） | — | Y 座標（可省略） |

### `/input mouse drag <x1> <y1> <x2> <y2> [button]`

拖曳

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `x1` | 整數（-65535–65535） | ✅ | 起點 X |
| `y1` | 整數（-65535–65535） | ✅ | 起點 Y |
| `x2` | 整數（-65535–65535） | ✅ | 終點 X |
| `y2` | 整數（-65535–65535） | ✅ | 終點 Y |
| `button` | 選項：`左鍵` / `右鍵` / `中鍵` | — | 要按哪個鍵（預設 `left`） |

### `/input mouse move <x> <y>`

移動游標

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `x` | 整數（-65535–65535） | ✅ | X 座標 |
| `y` | 整數（-65535–65535） | ✅ | Y 座標 |

### `/input mouse pos`

目前游標座標

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/input mouse scroll <amount> [x] [y]`

捲動（正值往上）

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `amount` | 整數（-1000–1000） | ✅ | 格數，正值往上、負值往下 |
| `x` | 整數（-65535–65535） | — | X 座標（可省略） |
| `y` | 整數（-65535–65535） | — | Y 座標（可省略） |

### `/input mouse up [button] [x] [y]`

放開滑鼠鍵

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `button` | 選項：`左鍵` / `右鍵` / `中鍵` | — | 要放開哪個鍵（預設 `left`） |
| `x` | 整數（-65535–65535） | — | X 座標（可省略） |
| `y` | 整數（-65535–65535） | — | Y 座標（可省略） |
