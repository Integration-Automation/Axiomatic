# `/sys` — 健檢 / 診斷 / 稽核 / 磁碟 / 更新 / 重啟

> 本目錄由指令樹產生，**唯一真實來源是程式碼**；bot 內建的 `/help` 才是
> 即時的那一份。指令一律是原生斜線指令——打 `/` 就會自動補全，參數在送出
> 前就有型別與值域檢查。
>
> **回覆一律泛用**：不提外部服務名稱、不露出主機路徑或檔名、不回傳原始
> 錯誤字串，完整細節只進 log。寫新指令時要沿用。

🔒 **限頻道**：只在 `bot_config.json` 的 `channel_id` 所指頻道回應（**擁有者可跨頻道**）。

共 **14** 個子指令。

---

## 直接子指令

### `/sys audit [n] [grep]`

最近的操作稽核紀錄

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `n` | 整數（1–50） | — | 筆數（預設 20、上限 50） |
| `grep` | 文字 | — | 只列 head 含此子字串的條目 |

### `/sys backfill_paths [limit] [apply]`

把本頻道舊訊息裡的目錄補成完整顯示

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `limit` | 整數（1–500） | — | 往回掃幾則訊息（預設 200、上限 500） |
| `apply` | 是／否 | — | 真的寫入（預設只試算、不動訊息） |

### `/sys cleanup_debug`

刪除所有除錯截圖

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/sys dashboard`

本機唯讀儀表板的網址

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/sys disk`

輸出與設定檔目錄的磁碟用量

權限：viewer 以上

### `/sys doctor`

非破壞式排障摘要

權限：viewer 以上

### `/sys git_pull [noreboot]`

拉取更新（預設成功後重啟）

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `noreboot` | 是／否 | — | 拉完不要自動重啟 |

拉成功之後會自動重啟（帶 `noreboot` 就不會）。bot 沒有監督者、或判斷不出來時，pull 照樣算數，
只是不自動重啟，新程式要等 bot 下次啟動才載入。工作樹有未 commit 的改動時會拒絕，
但四份佇列檔不算——批次一直在改寫它們；拉下來的更新若真的碰到佇列檔，git 自己會拒絕。

### `/sys health [mode]`

綜合健康檢查

權限：viewer 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `mode` | 選項：`完整` / `精簡` | — | quick = 只回核心狀態 |

### `/sys introspect_dom`

要求背景程式 dump 目前頁面的輸入欄位

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/sys metrics`

執行期指標（uptime / 次數 / 錯誤）

權限：viewer 以上

### `/sys probe_status`

狀態偵測的根因摘要

權限：viewer 以上

### `/sys restart`

結束行程讓監督者重新啟動

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

只有在 bot 是由主機上的啟動器帶起來的時候才會重啟。直接從編輯器執行的 bot 沒有人會把它
拉回來，這時重啟只會把 bot 關掉，所以會拒絕並說明；判斷不出來時也不重啟。

### `/sys undo`

撤回最近一次破壞性寫入

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

### `/sys update_check`

比對本機與遠端的版本落差

權限：operator 以上
