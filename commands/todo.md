# `/todo` — 產圖佇列：新增 / 列出 / 刪除 / 排序

> 本目錄由指令樹產生，**唯一真實來源是程式碼**；bot 內建的 `/help` 才是
> 即時的那一份。指令一律是原生斜線指令——打 `/` 就會自動補全，參數在送出
> 前就有型別與值域檢查。
>
> **回覆一律泛用**：不提外部服務名稱、不露出主機路徑或檔名、不回傳原始
> 錯誤字串，完整細節只進 log。寫新指令時要沿用。

🔒 **限頻道**：只在 `bot_config.json` 的 `channel_id` 所指頻道回應（**擁有者可跨頻道**）。

共 **33** 個子指令。

---

## 直接子指令

### `/todo dedupe <queue>`

移除重複條目（保留第一筆）

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `queue` | 選項：`主提示詞` / `角色1` / `角色2` / `負面提示詞` | ✅ | 要操作的佇列 |

**角色2 佇列有空白列時會拒做。** 該佇列是按位置配對的：第 N 列配第 N 對，
空白列代表「這一對不要角色2」。去重是整批刪列，刪掉任何一列都會讓後面每一
列往前位移；而空白列彼此看起來完全一樣，會被當成重複項清掉。要刪特定列請用
`/todo char2 remove`。

### `/todo duplicate <queue> <index> [n]`

複製某一筆 n 次

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `queue` | 選項：`主提示詞` / `角色1` / `角色2` / `負面提示詞` | ✅ | 要操作的佇列 |
| `index` | 整數（1–9999） | ✅ | 條目編號 |
| `n` | 整數（1–50） | — | 複製幾份（預設 1） |

### `/todo find <queue> <text>`

在佇列內搜尋（不分大小寫）

權限：viewer 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `queue` | 選項：`主提示詞` / `角色1` / `角色2` / `負面提示詞` | ✅ | 要搜尋的佇列 |
| `text` | 文字 | ✅ | 要找的字串 |

### `/todo move <queue> <index> <where>`

把某一筆搬到最前或最後

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `queue` | 選項：`主提示詞` / `角色1` / `角色2` / `負面提示詞` | ✅ | 要操作的佇列 |
| `index` | 整數（1–9999） | ✅ | 條目編號（1 起算） |
| `where` | 選項：`最前` / `最後` | ✅ | 搬到最前或最後 |

### `/todo shuffle <queue>`

打亂佇列順序

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `queue` | 選項：`主提示詞` / `角色1` / `角色2` / `負面提示詞` | ✅ | 要操作的佇列 |

洗牌只重排目前這一個佇列，不會連動其他佇列——原本第 N 對的組合會就此拆開。
角色2 佇列的空白列（「這一對不要角色2」）數量不變，但會落到別的位置。

### `/todo swap <queue> <i> <j>`

對調兩筆的位置

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `queue` | 選項：`主提示詞` / `角色1` / `角色2` / `負面提示詞` | ✅ | 要操作的佇列 |
| `i` | 整數（1–9999） | ✅ | 第一筆編號 |
| `j` | 整數（1–9999） | ✅ | 第二筆編號 |

## `/todo char1` — 角色1佇列

### `/todo char1 add [text]`

在角色1佇列後面新增一筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | — | 條目內容（多筆用 \ 分隔） |

### `/todo char1 addx3 [text]`

新增到角色1 佇列並重複 ×3

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | — | 要重複 3 次寫入的條目 |

### `/todo char1 clear`

清空角色1佇列

權限：operator 以上

### `/todo char1 list`

列出角色1佇列的所有條目

權限：operator 以上

### `/todo char1 pop`

刪除角色1佇列最後一筆

權限：operator 以上

### `/todo char1 remove <index>`

刪除角色1佇列第 i 筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `index` | 整數（1–9999） | ✅ | 要刪除的編號（1 起算） |

## `/todo char2` — 角色2佇列

### `/todo char2 add [text]`

在角色2佇列後面新增一筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | — | 條目內容（多筆用 \ 分隔） |

### `/todo char2 clear`

清空角色2佇列

權限：operator 以上

### `/todo char2 default`

把角色2 預設內容當一筆推進佇列

權限：operator 以上

### `/todo char2 list`

列出角色2佇列的所有條目

權限：operator 以上

### `/todo char2 pop`

刪除角色2佇列最後一筆

權限：operator 以上

### `/todo char2 remove <index>`

刪除角色2佇列第 i 筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `index` | 整數（1–9999） | ✅ | 要刪除的編號（1 起算） |

## `/todo negp` — 負面提示詞佇列

### `/todo negp add [text]`

在負面提示詞佇列後面新增一筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | — | 條目內容（多筆用 \ 分隔） |

### `/todo negp clear`

清空負面提示詞佇列

權限：operator 以上

### `/todo negp list`

列出負面提示詞佇列的所有條目

權限：operator 以上

### `/todo negp pop`

刪除負面提示詞佇列最後一筆

權限：operator 以上

### `/todo negp remove <index>`

刪除負面提示詞佇列第 i 筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `index` | 整數（1–9999） | ✅ | 要刪除的編號（1 起算） |

## `/todo prompt` — 主提示詞佇列

### `/todo prompt add [text]`

在主提示詞佇列後面新增一筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | — | 條目內容（多筆用 \ 分隔） |

### `/todo prompt clear`

清空主提示詞佇列

權限：operator 以上

### `/todo prompt default`

把預設提示詞當一筆推進佇列

權限：operator 以上

### `/todo prompt end`

追加終止標記，跑到那筆就乾淨收工

權限：operator 以上

### `/todo prompt insert <index> <text>`

把內容插進主提示詞佇列第 i 筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `index` | 整數（1–9999） | ✅ | 插入位置（1 起算；填「最後一筆 +1」等於附加） |
| `text` | 文字 | ✅ | 要插入的條目內容（多筆用 \ 分隔，依序插入） |

### `/todo prompt list`

列出主提示詞佇列的所有條目

權限：operator 以上

### `/todo prompt pop`

刪除主提示詞佇列最後一筆

權限：operator 以上

### `/todo prompt remove <index>`

刪除主提示詞佇列第 i 筆

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `index` | 整數（1–9999） | ✅ | 要刪除的編號（1 起算） |

### `/todo prompt template [name]`

把指定範本當一筆推進佇列

權限：operator 以上

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | — | 範本名稱（留空 = 列出可用範本） |

### `/todo prompt unend`

移除所有終止標記，讓佇列跑完整批

權限：operator 以上
