# `/locate` — 用文字 / 圖片 / 元素名稱在畫面上定位（限擁有者）

> 本目錄由指令樹產生，**唯一真實來源是程式碼**；bot 內建的 `/help` 才是
> 即時的那一份。指令一律是原生斜線指令——打 `/` 就會自動補全，參數在送出
> 前就有型別與值域檢查。
>
> **回覆一律泛用**：不提外部服務名稱、不露出主機路徑或檔名、不回傳原始
> 錯誤字串，完整細節只進 log。寫新指令時要沿用。

🔒 **限頻道**：只在 `bot_config.json` 的 `channel_id` 所指頻道回應（**擁有者可跨頻道**）。

共 **14** 個子指令。

---

## 直接子指令

### `/locate gone <kind> <target> [seconds]`

等某個東西從畫面上消失

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `kind` | 選項：`文字` / `視窗` / `圖片` | ✅ | 要等哪一種目標消失 |
| `target` | 文字 | ✅ | 目標內容 |
| `seconds` | 整數（1–600） | — | 最長等幾秒（預設 `30`） |

### `/locate pixel <x> <y> <color> [invert] [seconds] [tolerance]`

等某一點變成（或不再是）指定顏色

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `x` | 整數（-65535–65535） | ✅ | X 座標 |
| `y` | 整數（-65535–65535） | ✅ | Y 座標 |
| `color` | 文字 | ✅ | #RRGGBB |
| `invert` | 是／否 | — | 改成等它「不等於」該顏色 |
| `seconds` | 整數（1–600） | — | 最長等幾秒（預設 `30`） |
| `tolerance` | 整數（0–255） | — | 容許誤差 |

## `/locate image` — 用樣板圖片定位（不需辨識引擎）

### `/locate image click <template> [threshold]`

找到樣板圖片就點它的中心

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `template` | 附件 | ✅ | 要比對的樣板圖片 |
| `threshold` | 數字（0.1–1.0） | — | 相似度門檻（預設 0.9） |

### `/locate image find <template> [threshold]`

找出畫面上所有命中處，不點下去

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `template` | 附件 | ✅ | 要比對的樣板圖片 |
| `threshold` | 數字（0.1–1.0） | — | 相似度門檻（預設 0.9） |

### `/locate image wait <template> [seconds] [threshold]`

等樣板圖片出現在畫面上

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `template` | 附件 | ✅ | 要等的樣板圖片 |
| `seconds` | 整數（1–600） | — | 最長等幾秒（預設 `30`） |
| `threshold` | 數字（0.1–1.0） | — | 相似度門檻（預設 0.9） |

## `/locate text` — 用畫面文字定位（需辨識引擎）

### `/locate text click <text> [lang] [shot]`

找到畫面上的文字就點下去

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要點的文字 |
| `lang` | 文字 | — | 辨識語言代碼 |
| `shot` | 是／否 | — | 完成後附一張截圖 |

### `/locate text find <text> [region] [lang]`

找文字並回座標與信心值

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要找的文字 |
| `region` | 文字 | — | 限定區域「x y 寬 高」 |
| `lang` | 文字 | — | 辨識語言代碼（留空 = 自動判斷） |

### `/locate text wait <text> [seconds] [lang]`

等某段文字出現在畫面上

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要等的文字 |
| `seconds` | 整數（1–600） | — | 最長等幾秒（預設 `30`） |
| `lang` | 文字 | — | 辨識語言代碼 |

## `/locate ui` — 用元素名稱定位（首選：問作業系統要精確矩形）

### `/locate ui click <name> [type] [window] [shot]`

找到元素就點下去

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 元素名稱 |
| `type` | 文字 | — | 控制項型別（可省略） |
| `window` | 文字 | — | 限定視窗 |
| `shot` | 是／否 | — | 完成後附一張截圖 |

### `/locate ui find <name> [type] [window]`

找元素並回精確矩形

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 元素名稱 |
| `type` | 文字 | — | 控制項型別（可省略） |
| `window` | 文字 | — | 限定視窗（強烈建議帶，快 60 倍） |

### `/locate ui gone <name> [seconds] [type] [window]`

等元素消失

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 元素名稱 |
| `seconds` | 整數（1–600） | — | 最長等幾秒（預設 `30`） |
| `type` | 文字 | — | 控制項型別 |
| `window` | 文字 | — | 限定視窗 |

### `/locate ui read <name> [type] [window]`

讀元素現在的值

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 元素名稱 |
| `type` | 文字 | — | 控制項型別（可省略） |
| `window` | 文字 | — | 限定視窗 |

### `/locate ui tree [window]`

列出可點的元素

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `window` | 文字 | — | 限定視窗（不指定要掃最前面幾個視窗，慢很多） |

### `/locate ui wait <name> [seconds] [type] [window]`

等元素出現

**限擁有者**（閘門在派發前，**不受 `user_roles` 設定影響**）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `name` | 文字 | ✅ | 元素名稱 |
| `seconds` | 整數（1–600） | — | 最長等幾秒（預設 `30`） |
| `type` | 文字 | — | 控制項型別 |
| `window` | 文字 | — | 限定視窗 |
