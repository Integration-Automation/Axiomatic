# `/tool` — 編碼 / 雜項工具

> 本目錄由指令樹產生，**唯一真實來源是程式碼**；bot 內建的 `/help` 才是
> 即時的那一份。指令一律是原生斜線指令——打 `/` 就會自動補全，參數在送出
> 前就有型別與值域檢查。
>
> **回覆一律泛用**：不提外部服務名稱、不露出主機路徑或檔名、不回傳原始
> 錯誤字串，完整細節只進 log。寫新指令時要沿用。

🌐 **跨頻道**：bot 看得到的任何頻道都能用。

共 **8** 個子指令。

---

## 直接子指令

### `/tool base64 <text>`

Base64 編碼

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要編碼的文字 |

### `/tool color <value>`

色票圖（3 或 6 個 hex digits）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `value` | 文字 | ✅ | hex 色碼，例如 ff8800 或 f80 |

### `/tool hash <text>`

md5 / sha1 / sha256

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要雜湊的文字 |

### `/tool qr <text>`

產生 QR code 圖

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要編進 QR code 的內容 |

### `/tool say <text>`

讓 bot 代為發言（限 server owner）

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要讓 bot 說的內容 |

### `/tool unbase64 <text>`

Base64 解碼

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要解碼的 base64 字串 |

### `/tool urldecode <text>`

URL 解碼

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要解碼的文字 |

### `/tool urlencode <text>`

URL 編碼

| 參數 | 型別 | 必填 | 說明 |
|---|---|:--:|---|
| `text` | 文字 | ✅ | 要編碼的文字 |
